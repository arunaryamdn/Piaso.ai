{
  "version": 2,
  "buildCommand": "DISABLE_ESLINT_PLUGIN=true npm run build",
  "rewrites": [
    { "source": "/api/:path*", "destination": "/api/index" }
  ],
  "env": {
    "DB_PATH": "/tmp/portfolios.db",
    "AUTH_DB_PATH": "/tmp/users.db"
  }
}
